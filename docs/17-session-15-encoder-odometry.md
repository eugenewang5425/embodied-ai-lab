# 第十五课：小车走直线，里程计却觉得它在转弯

本课沿着第三阶段继续：不换仿真器、不增加自由度，只把上一课的**真实位姿**与**估计位姿**分开，再加入一种固定的测量比例偏差。

## 先用一句话理解里程计

编码器记录轮子转了多少；里程计把每一小段转动换成车体的前进和转向，再从已知起点逐步累加，推算现在的位置。

它不是直接“看见地图里的位置”。联系 GIS，可以把它想成从已知起点连续累计位移：如果每段的长度或方向有偏差，推算的终点就可能偏离真实位置。

本课仍然没有反馈控制：轮速按预设时间表执行。读数偏差只进入估计器，**不进入电机指令，也不改变真实轨迹**。界面上的虚线车是同一台车的估计轮廓，不是第二台真实小车。

## 1. 分清本课的四种数据

| 数据 | 从哪里来 | 用来做什么 |
| --- | --- | --- |
| 轮速指令 `[左, 右]`，rad/s | 预先设定的运动计划 | 驱动理想轮速运动学模型 |
| 真实轮角与真实位姿 | 无打滑、轮速立即实现的模型 | 生成模拟传感读数，以及实验评估；不喂给里程计当位置答案 |
| 编码器累计角度 `[左, 右]`，rad | 真实轮角加上指定比例偏差 | 里程计唯一的运动测量输入 |
| 估计位姿 `[x̂, ŷ, θ̂]` | 由相邻编码器角度差推算 | 显示位置、计算地标落图结果 |

里程计还已知初始位姿 `(0, 0, 0)`、轮半径 0.05 m 和轮间距 0.30 m。仅靠轮子转动，不能自动知道绝对起点。编码器角度连续累积，不折返到一圈内；本课没有脉冲计数量化、漏码、时延或随机噪声。

当前是理想模型，所以真实轮角由实际执行的轮速积分生成，不是读取真实硬件。估计器接口只接收编码器角度、初始位姿和几何尺寸，不接收轮速指令、真实位置或地标位置。实际系统也需要区分用反馈算里程计与直接积分命令；可参见 [ROS 2 差速控制器文档对反馈和 open_loop 的说明](https://control.ros.org/jazzy/doc/ros2_controllers/diff_drive_controller/doc/userdoc.html)。本项目未安装或使用该控制器。

## 2. 只改变一个因素：右轮读数比例

三组使用同一条真实轨迹、同一组轮速、同一初态：

| 组别 | 左轮读数 | 右轮读数 |
| --- | --- | --- |
| 0% 基线 | 真实轮角 × 1 | 真实轮角 × 1 |
| +1% | 真实轮角 × 1 | 真实轮角 × 1.01 |
| +2% | 真实轮角 × 1 | 真实轮角 × 1.02 |

这是模拟的右轮角度读数标定比例偏差，不是右轮真的更大、更快或发生了打滑。负转角也按同一系数缩放，不是每步额外加一个正数。

直行时两个轮子都以 4 rad/s 转动，每步 0.04 s：

```text
真实增量：左 0.1600 rad，右 0.1600 rad
+2% 读数：左 0.1600 rad，右 0.1632 rad
```

估计器不知道右轮多报了，于是以为右边走得更多，推断小车在向左转。这就是本课的因果链：

```text
右轮角度多报 → 估计左右行程不等 → 估计朝向偏左
             → 后续前进沿错误朝向累加 → 估计位置偏离
```

## 3. 从轮角增量到位置增量

相邻编码器读数相减，得到 `ΔφL、ΔφR`：

```text
左轮行程 ΔsL = r × ΔφL
右轮行程 ΔsR = r × ΔφR

车体行程 Δs = (ΔsL + ΔsR) / 2
朝向变化 Δθ = (ΔsR - ΔsL) / b
```

朝向更新为 `θ̂_next = θ̂ + Δθ`。位置更新沿这一小段圆弧，而不是只把 `Δs` 加到世界 X 上。实现使用：

```text
d = Δs × sin(Δθ/2)/(Δθ/2)     # Δθ=0 时比值取 1
x̂_next = x̂ + d cos(θ̂ + Δθ/2)
ŷ_next = ŷ + d sin(θ̂ + Δθ/2)
```

与上一课一样，每段轮速恒定，圆弧更新是解析的。状态记录 N+1 帧，轮速指令 N 段。估计器第 i 帧只使用截至第 i 帧的读数，不使用未来数据。

## 4. 实验结果：读数只多一点，位置不只多一点

路线 A：直行 12 s，真实小车前进 2.4 m，朝向一直为 0°。

路线 B：边长 0.8 m 的方形，每边直行 4 s、原地左转 2 s，重复四次，共 24 s。真实小车返回起点、累计转过 360°。这是预设运动完成，不是识别到闭环并自动修正。

正式记录：`results/mobile_odometry_2026-09-03/`。

| 路线 | 右轮偏差 | 终点位置误差 | 终点朝向误差 | 终点地标落图误差 |
| --- | --- | --- | --- | --- |
| 直行 2.4 m | 0% | <1e-10 m | 数值舍入量级 | <1e-10 m |
| 直行 2.4 m | +1% | 9.69 cm | +4.58° | 5.13 cm |
| 直行 2.4 m | +2% | **19.40 cm** | **+9.17°** | 10.01 cm |
| 方形一圈 | 0% | <1e-10 m | 数值舍入量级 | <1e-10 m |
| 方形一圈 | +1% | 7.72 cm | +7.91° | 11.20 cm |
| 方形一圈 | +2% | **15.24 cm** | **+15.82°** | 22.49 cm |

直行 +2% 组末尾估计位置约为 `(2.414, 0.194) m`，真值是 `(2.400, 0.000) m`。错误不只是把前进距离多算了 2%；主要还多出了一段实际不存在的横向位移。

方形 +2% 组末尾估计位置约为 `(0.121, -0.093) m`，累计估计朝向 375.82°，但真实位置已回原点、累计朝向 360°。界面保存连续朝向；“朝向误差”使用两方向之间最短的带符号角差，不把 359° 与 1° 当成差了 358°。

### 不要误解“累积误差”

方形路线的位置误差曲线有短暂下降，+2% 组有 80 个区间出现下降：不等于系统进行了定位修正，而是不同方向的误差暂时部分抵消。这里没有任何修正算法。看终点误差还不够，因此报告同时保留最大误差、RMS 和完整曲线。

0% 与真值几乎重合，只验证理想模型、精确参数和无误差读数下的实现一致性，不代表真实编码器定位可以达到这些数字。两条确定性路线也不能代表真实机器人上的误差分布或成功率。

## 5. 与上一课连接：公式没错，地标仍能放错

固定地标仍为世界 `(1.1, 0.8) m`，传感器安装仍为前 0.12 m、左 0.04 m、偏转 +30°。读数由**真实位姿**生成，再用**估计位姿**按完整 S→B→世界公式映射。

上一课的反例是“遗漏旋转与安装偏移，公式用错了”。本课没有遗漏这些变换，但位姿输入已经有偏差，所以绿色真实地标与彩色落图标记仍会分离。这是 GIS/建图中很重要的区分：数据转换流程正确，并不意味着用于转换的位置与朝向都正确。

地标位置只用于落图诊断和误差评估，不参与估计器纠偏；本课没有利用地标实现定位，没有地图优化或 SLAM。

## 慢放与观察顺序

在项目根目录的 PowerShell 中：

```powershell
uv run python -m embodied_learning.odometry_demo --results results/mobile_odometry_2026-09-03
```

默认暂停、0.25×、直行路线、右轮读数 +2%。直行完整播放约 48 s，方形约 96 s；嫌长可用“下一段”或时间轴跳转，不必一直等。

1. 点一次单步，读右侧的 `0.1600 → 0.1632 rad`。它是**刚完成的区间**；“下一步指令”是即将执行的区间，二者不要混淆。
2. 点播放，观察蓝色实车直走、紫色估计轮廓逐渐偏左。它们不是两台真实小车。
3. 拖到 12 s，读取 19.40 cm 和 9.17°；切成 0% 后仍停在同一个时刻，估计轮廓与真值重合。
4. 切到方形路线会回到起点暂停；“下一段”依次跳到直行/转向的边界。走完一圈，看真实车与估计位置是否同时回原点。
5. 底部切换位置、朝向、地标落图误差。绿 0%、橙 +1%、紫 +2% 总是叠加，共用当前路线的时间与纵轴刻度。

曲线显示完整记录和当前时刻游标，平面图只画已走轨迹。速度只影响回放，不改变仿真步长。终帧没有下一步轮速，但仍显示最后一个已经完成的编码器增量。关闭窗口或 Esc 退出。

## 复现与验收

```powershell
uv run python -m embodied_learning.experiments.mobile_odometry --output results/mobile_odometry_my_run
uv run pytest -q
uv run python -m ruff check src tests
uv run python -m ruff format --check src tests
```

输出须使用新目录，不覆盖旧结果。记录包含 `summary.json`、`trajectories.npz`、`comparison.png`，保存真实轮速、真实累计轮角、真实位姿、三组测量/估计位姿、地标坐标和逐帧误差；另保存配置、版本、生成源码哈希、轨迹文件哈希。估计器不覆盖真值数组。`results` 受 Git 忽略，需要长期保留时另行归档。

- 新增 31 项测试独立通过（最终 8.79 s），Ruff 静态及格式检查通过（53 个 Python 文件）。覆盖手算增量、正反转、初始位姿、编码器零点偏移、因果前缀一致性、三组共享真值、圆弧解析对照、误差与落图、文件损坏拒绝、回放同时间切换及可变时长。组合回归的限制见下段，不以首次成功覆盖后来失败。
- 第十四课保存的直行 4 s 真值与本课前 4 s 数组完全一致；旧轨迹哈希仍匹配。新结果生成源码和轨迹哈希已回读核验。
- 实际桌面使用 computer-use 核验：默认暂停、0.25×、直行、+2%；单步到 0.04 s 显示真实增量 0.1600 rad 与读数 0.1632 rad；播放中 7.56 s 可见蓝色直行与紫色估计分离，真值 x=1.512 m、估计约 `(1.525, 0.077) m`；12.00 s 显示位置误差 19.40 cm、朝向误差 9.17°、地标落图误差 10.01 cm，且明确没有下一步轮速。三色曲线、图例和底部说明完整可见，未发现需修改的布局问题。观察到下拉菜单出现用户交互后停止继续输入，保留窗口；方形路线、指标切换与同时间偏差切换由自动测试核验，未宣称全部完成桌面逐项操作。程序测试不替代桌面验收，也不替代学员理解；第十二课的旧桌面验收限制仍独立保留。

### 上轮组合回归问题与本轮处理

首次全量运行曾为 282 项通过（26.29 s），但收尾复跑时出现 **281 通过、1 失败**，并再次复现：`tests/test_arm_timing.py::test_tk_timing_view_constructs_and_changes_clock` 在 `tk.Tk()` 初始化时提示读不到 Tcl/Tk 的 `scale.tcl` 或 `auto.tcl`。错误发生在旧第十二课窗口创建之前，不是本课里程计数值断言。

只读检查发现被报告的库文件实际存在，Python 3.12 的目录连接也指向当前 3.12.13；该旧测试独立运行通过（3.50 s），不包含本课测试的原 251 项也通过（19.72 s）。但仅在当前子进程显式指定真实 Tcl/Tk 库路径，组合套件仍为 281 通过、1 失败（28.64 s）。因此不能归结为简单缺文件、声称环境已修好，或确定旧代码就是根因；组合运行触发原因尚未定位。

上轮未改动旧测试、跳过测试、重装环境或持久修改环境变量。实验和第十五课独立窗口可运行，但当时的组合验收仍有此限制，不能把两批分开通过等同于全套共同通过。

本轮补记：显式循环引用回收不能稳定解决；改为每项真实 Tk 测试在独立 Python 子进程执行，保留全部断言、超时与失败传播，不跳过或重试。282 项组合连续通过两次，加入第十六课后 313 项连续通过两次。此为测试进程隔离的工程方案，底层 Tcl 异常原因仍未确定；完整证据见[第十六课](18-session-16-encoder-calibration.md)。

## 理论对应（Embodied-AI-Guide）

在 [Embodied-AI-Guide](https://github.com/TianxingChen/Embodied-AI-Guide) 的地图里，本课站在（6）Control 篇·机器人学导论（6.3.3 里程计与 SLAM）的入口：轮式里程计是航位推算（dead reckoning）的机器人版——把每一步增量测量递推积分，误差只进不出。6.3.3 后面的 SLAM、滤波、图优化条目，都是在回答"这种只进不出的误差怎么用其他信息约束"；本课先把没有任何外部信息时误差的规律量化出来。

核心现象是"小比例偏差经递推放大成大位置偏差"。右轮每步多报 2%，直行 2.4 m 后终点位置误差 19.40 cm——而里程本身只多算了 2.4 m × 2% = 4.8 cm。多出的部分来自因果链的第二段：估计朝向每步偏一点，后续位移都沿错误的朝向累加。朝向误差近似随时间线性增长到 +9.17°，横向漂移 ≈ 距离 × 平均朝向误差 ≈ 2.4 m × 4.6° ≈ 19 cm，与估计终点 y 偏差 0.194 m 对上。方形 +2% 组 15.24 cm、朝向 +15.82°：转四次弯，方向误差被递推"记住"四次。这种与几何耦合的确定性放大，是 6.3.3 里里程计误差模型的标准内容。

另一个要紧的理论区分在本课已经埋下：固定比例偏差是**确定性**的，可以像下一课那样用独立基准标定；而"误差曲线有 80 个区间短暂下降"提醒我们，不同方向误差的暂时抵消不是修正——递推系统里没有任何机制在"纠错"。把这两件事分开，才谈得上状态估计里"预测的误差该由观测来修"。

本课的四种数据划分（指令、真值、读数、估计）也是 6.3.3 实验的标准纪律：估计器只收编码器角度、初态和几何参数，不收真值——第二十课把它落成 ROS 消息时同样如此。落图误差（直行 +2% 组 10.01 cm）小于位置误差（19.40 cm）则说明：数据转换流程正确 ≠ 用于转换的位姿正确，这与第十四课"公式没错、输入错了"是同一条边界的两侧。

## 思考题

1. 直行 +2% 组终点位置误差 19.40 cm，但轮程只多算了 2.4 m × 2% ≈ 4.8 cm。多出的十几个厘米从哪来？用终点朝向误差 +9.17°（近似随时间线性增长，取平均值）估算"距离 × 平均朝向误差"的量级，看能否对上。
   提示：第 4 节"估计位置约 (2.414, 0.194) vs 真值 (2.400, 0.000)"与第 2 节因果链。
2. 方形 +2% 组累计估计朝向 375.82°，即每圈多转约 15.82°。四条边都是直行，朝向误差是哪一段产生的？
   提示：第 2 节因果链——右轮多报让估计器在**直行段**也以为小车在缓缓左转。
3. +2% 组方形路线有 80 个区间位置误差下降。为什么这不能算"定位修正"？如果换成随机噪声（下一课），这类下降还会出现吗？
   提示：第 4 节"不要误解'累积误差'"——看看此时有没有任何机制在读取外部信息。
4. 0% 基线组终点误差 <1e-10 m。这个数字证明了什么、明确不能证明什么？
   提示：第 4 节"0% 与真值几乎重合，只验证……实现一致性，不代表真实编码器定位可以达到这些数字"。
5. 估计器接口为什么只接收编码器角度、初始位姿和几何尺寸，不接收轮速指令或地标位置？如果让它偷看真值或指令，本课还能说明"里程计误差会积累"吗？
   提示：第 1 节四种数据表格与文内 ROS diff_drive_controller 对 feedback 与 open_loop 的说明。

## 本课停止点

先回答：**为什么编码器多报一点转角，会同时影响位置、朝向和地标落图？为什么更复杂的坐标转换公式不能直接消掉这种误差？**

工程上已采用上述隔离方案恢复组合测试；下一课[标定固定比例偏差](18-session-16-encoder-calibration.md)已实现：用独立测距数据求系数，再用未参与标定的路线检验效果。当前问题是确定性的读数比例错误，不急着引入滤波器或 SLAM；随机噪声、打滑和外部定位修正留到有明确问题时逐项加入。
