# 固定距离试拍检查

## 结果

- 会话：`calibration/sessions/session_20260902_113833_342289`。
- 实际保存 9 张，全部 3840×2160，逐张锁焦记录通过；目标及取帧前后读回值均为 293。
- view_000–005、008 检测到全部 24 个内角点；view_006、007 为 22 个。
- 按现有同角点 ID、平均位置差小于 5 px 的规则，没有近重复照片。
- 已检查全部 9 张棋盘局部预览，并查看 view_000 和 view_006 原分辨率局部。
- 中央照片的棋格边界可辨，暂不建议仅为清晰度继续改变距离；view_006（左上位置）仍明显发虚、对比度偏低。
  不能仅凭这些照片区分离焦、运动、照明或镜头边缘成像问题，也未独立尺量实际距离。
- 位置已覆盖中心与四角；中央多为接近正视的姿态，下一步应增加温和的左右/上下倾斜。

这只是采集预检查，9 张少于当前解算器要求的至少 12 张，未进行完整内参拟合，
未输出新的重投影 RMS，未声明标定通过，未创建默认相机参数。
22 个角点仍超过采集最低数量；未据此删除照片。

## 继续采集前需要核对

之后还发现 `session_20260902_114119_649338`：只有会话记录，检查时为 0 张照片。
这个新会话启动时报告自动对焦开启、焦点 296，不能把它当成前面锁在 293 的同一个会话。
检查时未发现命令行匹配采集入口的运行进程，需用户确认当前窗口状态。

若原窗口仍为 Saved 9、LOCKED、value 293，可不改焦点，继续补充不同倾角和中间边缘位置。
若已关闭或窗口变成 Saved 0，不应直接假设新拍照片可以与这 9 张合并，需核对恢复后的焦点及其他相机设置。
同理，本组焦点 293 与上一组 286 不同，未混合处理。

## 产物

- `outputs/calibration-trial-20260902-113833/trial.json`：逐张焦点、角点、尺寸、覆盖及近重复检查。
- 同目录 `contact_1.jpg`：9 张棋盘局部预览。
- 同目录 `crop_view_000.png` 至 `crop_view_008.png`：未缩放的原图局部，仅供检查。
- `scripts/check_capture_trial.py`：小样本预检查入口；拒绝覆盖既有输出目录，不拟合内参、不改原图。

脚本在这 9 张真实照片上完成执行，Ruff 检查通过。未修改相机采集程序或质量门槛。
